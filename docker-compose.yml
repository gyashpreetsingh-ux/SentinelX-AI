version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sentinelx-backend
    ports:
      - "8088:8088"
    environment:
      - PORT=8088
      - HOST=0.0.0.0
      - DATABASE_URL=sqlite:///./sentinelx.db
      - JWT_SECRET=sentinelx-cyber-defense-jwt-secret-key-2026-yashpreet
      - CORS_ORIGINS=http://localhost:5188,http://localhost:3100,http://127.0.0.1:5188
    volumes:
      - ./backend/sentinelx.db:/app/sentinelx.db
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: sentinelx-frontend
    ports:
      - "3100:80"
    depends_on:
      - backend
    restart: unless-stopped
